# Multi-Agent Neural Network for Medicine

## 🏗️ Архитектура проекта
- Мульти-агентная система
- Модуль RAG (Retrieval-Augmented Generation)
- Векторная база медицинских знаний
- Интерфейс врача

## 📁 Структура данных

### Обработанные данные
Все обработанные медицинские документы находятся в `data/processed/`:
```
data/processed/cardiology/
├── Articles/ # Научные статьи по кардиологии
├── Textbooks/ # Учебники
├── Guidelines/ # Клинические рекомендации
├── Handbooks/ # Справочники
├── Cases/ # Клинические случаи
└── Ontologies/ # Онтологии
```

### Формат данных
Каждый документ в формате `.txt` содержит:
1. **Метаданные** в JSON
2. **Текст документа** в UTF-8

Пример загрузки:
```python
from pathlib import Path
import json

def load_document(file_path):
    with open(file_path, 'r', encoding='utf-8') as f:
        content = f.read()
    
    if '=== СОДЕРЖАНИЕ ===' in content:
        metadata_str, text = content.split('=== СОДЕРЖАНИЕ ===', 1)
        metadata = json.loads(metadata_str.replace('=== МЕТАДАННЫЕ ===', '').strip())
        return metadata, text.strip()
    return {}, content
```
## 🚀 Начало работы

1. Клонируйте репозиторий

2. Установите зависимости: pip install -r requirements.txt

3. Загрузите данные для RAG-модуля